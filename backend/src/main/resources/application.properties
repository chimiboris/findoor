spring.application.name=mapiol-backend

spring.datasource.url=jdbc:postgresql://localhost:5432/mapiol
spring.datasource.username=mapiol
spring.datasource.password=mapiol
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=30MB

server.port=8081

management.endpoints.web.exposure.include=health,info
# Le mail (MailHog en dev, cf. docker-compose.yml) est optionnel : son indisponibilite
# ne doit pas faire tomber la sante globale de l'application.
management.health.mail.enabled=false

# Cle de developpement uniquement -- remplacee par une variable d'environnement en production.
mapiol.jwt.secret=dev-only-change-me-mapiol-secret-key-please-override-in-env-min-32-bytes
mapiol.jwt.access-token-minutes=30
mapiol.jwt.refresh-token-days=14

mapiol.cors.allowed-origins=http://localhost:4200

mapiol.storage.endpoint=http://localhost:9000
mapiol.storage.bucket=mapiol-media
mapiol.storage.access-key=minioadmin
mapiol.storage.secret-key=minioadmin

mapiol.payment.provider=cinetpay
mapiol.payment.cinetpay.site-id=CHANGE_ME
mapiol.payment.cinetpay.api-key=CHANGE_ME
mapiol.payment.cinetpay.notify-url=http://localhost:8081/api/paiements/webhook/cinetpay
mapiol.payment.cinetpay.return-url=http://localhost:4200/proprietaire/annonces
