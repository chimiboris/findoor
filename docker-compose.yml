services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mapiol
      POSTGRES_USER: mapiol
      POSTGRES_PASSWORD: mapiol
    ports:
      - "5432:5432"
    volumes:
      - mapiol_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mapiol"]
      interval: 5s
      timeout: 5s
      retries: 10

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - mapiol_minio_data:/data

  mailhog:
    image: mailhog/mailhog:latest
    ports:
      - "1025:1025"
      - "8025:8025"

volumes:
  mapiol_postgres_data:
  mapiol_minio_data:
